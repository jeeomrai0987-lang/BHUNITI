/*
 * English strings for src/pages/admin/DistrictGis.jsx -- the district command
 * map.
 *
 * Reached from a component as t("pages.districtGis....").
 */

const districtGis = {
  badge: "District command map",
  division: "Ghaziabad GIS spatial division",
  title: "Cadastral & parcel intelligence centre",
  // The two filters keep their visible caption, which is a real <label> now,
  // so the colon lives in the string.
  tehsil: "Tehsil:",
  village: "Village:",
};

export default districtGis;
